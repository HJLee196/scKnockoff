% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_toy_seurat_ad.R
\name{make_toy_seurat_ad}
\alias{make_toy_seurat_ad}
\title{Generate a toy Seurat object for differential analysis}
\usage{
make_toy_seurat_ad(
  n_genes = 1000,
  donors_per_group = 6,
  cell_types = c("Astro", "Micro", "Neuron"),
  cells_per_donor_per_type = 50,
  n_batch = 2,
  n_signals = 50,
  signal_strength = 2,
  r = 5,
  sigma = 0.5,
  mean_umi = 5000,
  lib_size_sd = 0.4,
  age_effect_sd = 0.05,
  batch_effect_sd = 0.15,
  cell_type_effect_sd = 0.25,
  loading_prop = 0.2,
  loading_sd = 0.7,
  normalize = TRUE,
  scale_data = TRUE,
  seed = 1
)
}
\arguments{
\item{n_genes}{Number of genes.}

\item{donors_per_group}{Number of donors in each disease group.}

\item{cell_types}{Character vector of cell types.}

\item{cells_per_donor_per_type}{Number of cells per donor and cell type.}

\item{n_batch}{Number of batches.}

\item{n_signals}{Number of ground-truth disease-associated genes.}

\item{signal_strength}{Fold change for disease-associated genes on the
expression scale.}

\item{r}{Number of latent factors.}

\item{sigma}{Standard deviation of Gaussian noise in the log-mean expression
layer.}

\item{mean_umi}{Target mean library size for the generated count matrix.}

\item{lib_size_sd}{Standard deviation of the log-library-size factor.}

\item{age_effect_sd}{Standard deviation of age effects across genes.}

\item{batch_effect_sd}{Standard deviation of batch effects across genes.}

\item{cell_type_effect_sd}{Standard deviation of cell-type effects across genes.}

\item{loading_prop}{Proportion of genes with nonzero loadings for each latent
factor.}

\item{loading_sd}{Standard deviation of nonzero latent-factor loadings.}

\item{normalize}{Logical; whether to run \code{Seurat::NormalizeData()}.}

\item{scale_data}{Logical; whether to run \code{Seurat::ScaleData()}.}

\item{seed}{Random seed. If \code{NULL}, the random seed is not set.}
}
\value{
A Seurat object containing simulated UMI-like counts and cell-level metadata.
The ground-truth signal indices are stored in \code{object@misc$true_signal},
and the names of the ground-truth signal genes are stored in
\code{object@misc$true_signal_genes}. The object also stores simulation
components such as \code{B0}, \code{B1}, \code{A}, \code{X}, \code{G},
\code{mu}, and the observed library sizes in \code{object@misc}.
}
\description{
This function generates a toy single-cell RNA-seq dataset with disease
labels, observed covariates, latent factors, and ground-truth signal genes.
The simulated latent expression layer follows
\eqn{G = X B_0 + A B_1 + E}, where \eqn{G} is interpreted as a Gaussian
log-mean expression matrix. Observed UMI-like counts are generated from
\eqn{Y_{ij} \sim Poisson(\mu_{ij})}, where
\eqn{\mu_{ij} = \exp(G_{ij})}, up to cell-specific library size scaling.
}
